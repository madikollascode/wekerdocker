build:
  box: google/golang
  steps:
    # Test the project
    - internal/docker-build: 
        dockerfile: Dockerfile 
        image-name: brahim777/monero # name used to refer to this image until it's pushed   
    - internal/docker-run:
        image: brahim777/monero
        name: myTestContainer
